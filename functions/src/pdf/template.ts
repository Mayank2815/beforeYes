// Re-export from the shared template
// In production, copy the full template here or share via a local package
// For the purposes of this build, we inline a reference to the actual template

export { buildReportHTML } from '../../../src/lib/pdf/template'
